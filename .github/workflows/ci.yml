name: Logwild CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - name: Install just
        run: sudo apt-get update && sudo apt-get install -y just zsh
      - name: Run linters
        run: |
          just fmt
          just tidy
          just vet
      - name: Run tests
        run: just test
      - name: Build
        run: just build
